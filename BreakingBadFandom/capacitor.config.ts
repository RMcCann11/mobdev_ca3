import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'BreakingBadFandom',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
